export interface RoomPaginationData {
  page: number,
  limit: number,
  filter: string,
}

export interface ClientPaginationData {
  page: number,
  limit: number,
  filter: string,
}

export interface UserPaginationData {
  page: number,
  limit: number,
  filter: string,
}

export interface ReservationPaginationData {
  page: number,
  limit: number,
  filter: string,
}
